<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.libAPI"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="17" />

    <!-- permission cannot refer to strings in name attribute -->
    <permission android:name="com.example.acl.svc.ACCESS" />

    <uses-permission android:name="com.example.acl.svc.ACCESS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name="TestActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.example.spServices.spService"
            android:exported="true"
            android:permission="com.example.acl.svc.ACCESS"
            android:process=":remote" >
            <intent-filter>
                <action android:name="com.example.svc.Logger" />
            </intent-filter>
        </service>
    </application>

</manifest>